# Pwn Trainging Hands-on

Pwn 問題用のハンズオンのコード集

## コンパイルについて
- 基本は `make` を用いたコンパイルで自動化されるようになっています
- 手動で gcc コマンドを動かす場合は, 成果物がプロジェクト直下の `/build` ディレクトリに配置されるようなコマンドをコメントで書いているので, 適宜ディレクトリを作成するなどしてコンパイルしてください